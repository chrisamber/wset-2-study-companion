VOYAGE_API_KEY=
# Get this from the Vercel dashboard's AI Gateway > API Keys page.
# Required in production too — Vercel's OIDC auto-auth did not work in
# practice for this project, so set this as a Vercel env var as well.
AI_GATEWAY_API_KEY=
# Optional overrides — see Global Constraints in the implementation plan:
# SOMMELIER_MODEL=anthropic/claude-sonnet-4-6
# VOYAGE_MODEL=voyage-4-lite
